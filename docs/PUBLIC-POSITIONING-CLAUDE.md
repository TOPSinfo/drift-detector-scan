# Public positioning — Claude plugin first (ACTIVE)

Unparked 2026-08-15. README + marketplace/plugin blurbs follow this brief.
Related (still parked): `docs/FRONTEND-PLANE.md` — do not start HTML hero work from this doc.

## Audience
General public via **GitHub + Claude Code plugin**. Company needs a ship that
**uses AI** and **solves a real problem**. `drift-scan` / fleet CI is a later story,
not the homepage.

## Locked decisions
- Hero surface = **Claude plugin** (`/drift-detector`), not CLI install.
- Boast frame: *AI proposes. The scanner adjudicates.*
- Tagline stays *Know before it breaks.* Sub: Claude finds what the scan can’t
  see yet; Drift Detector proves it (file:line, sourced dates, or honest needs-human).
- Demo order: blind → Claude (resolve/shape) → proof (gate / catalog / verify).
- Never claim “AI-powered scanner” as if the scan path is the model.
- Never invent dates; empty AI Frontier says “no pass,” not clean.
- Visual: receipt theater + amber encore — not purple-glow AI chrome.
- Deferred: CI/`drift-scan` as primary public path; Vue restyle; full `report.html`
  unless unparked with FRONTEND-PLANE.

## Shipped
1. README hero / marketplace blurb — done (2026-08-15).
2. In-chat lead with `summary.html`; cockpit = drill-down — done.
3. CI as a one-line footnote only — done.
4. Still deferred: FRONTEND-PLANE / `summary.html` lead.

## Do not
- Inflate AI Frontier counts into certified-looking fixes.
- Put client hosts in public `agent/*.yaml`.
- Force-push stale `v1.0-cut` without remaking from current master.
